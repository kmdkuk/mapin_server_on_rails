ファイル追加<br>
<%= link_to "new", new_company_shop_file_path(@company, @shop, @files) %><br><br>

<table>
  <tr>
    <th>id</th>
    <th>ファイル名</th>
    <th>ファイルタイプ</th>
  </tr>

  <% @files.each do |file| %>
    <%= render 'show_data', file: file %>
  <% end %>

</table>
